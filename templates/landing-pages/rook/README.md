# ROOK

Serve `site/` with a local HTTP server. The working page is `site/index.html`. `PROMPT.md` describes the completed original implementation; the source and supplied asset licenses are included.
